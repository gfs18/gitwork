<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yc.ht.mapper.SpecialMapper">
	<select id="findSpecial" resultType="Special">
		select * from Special <!-- where spid=#{spid},sgid=#{sgid},spname=#{spname},
		language=#{language},sppicPath=#{sppicPath},sppubTime=#{sppubTime} -->
	</select>
	
	
</mapper>